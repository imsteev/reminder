# Database configuration
DATABASE_URL=postgres://user:password@localhost:5432/reminder_app?sslmode=disable

# Server configuration  
PORT=8080

# Configuration is managed by go-envconfig
# Defaults are defined in backend/config/config.go
# You can override any values by setting environment variables